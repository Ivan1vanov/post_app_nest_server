import { GeneratedTokens } from '../../../../modules/auth';

export const tokensMock: GeneratedTokens = {
  accessToken: 'header.payload.signature',
  refreshToken: 'header.payload.signature',
};
